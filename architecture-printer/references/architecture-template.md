# {项目名} 架构速查手册

> 生成日期：{date} ｜ 扫描范围：{scope} ｜ 文件：{files} ｜ 行数：{lines} ｜ 函数/类：{declarations} ｜ 已识别公开入口覆盖率：{coverage}

## 架构总览

用 2–4 段话回答：项目解决什么问题、用户/前端从哪里进入、核心编排对象是谁、结果如何返回。只写扫描结果或源码/项目文档能证明的内容；不确定处写 `[待确认: ...]`。

## 技术栈

- 前端：框架、语言、构建工具和版本证据。
- 后端：框架、语言、运行方式和版本证据。
- 数据与部署：数据库、缓存、队列、容器/CI/部署配置。

## 核心链路图解

链接到 `architecture-workflow.html`，并补一段 Mermaid 或文字链路：

```text
入口 → 编排 → 执行/外部依赖 → 数据层 → 返回
```

说明同步实线、异步虚线、内部/外部颜色的含义；没有证据的边不要绘制成确定关系。

## 模块索引

| 模块 | 职责 | 关键文件 | 入口函数 |
| --- | --- | --- | --- |
| {module} | {responsibility} | `{file:line}` | `{function}` |

正文补充模块之间的依赖方向、状态传递和关键数据结构，避免只堆表格。

## 外部依赖

| 服务 | 用途 | 鉴权方式 | 是否必需 |
| --- | --- | --- | --- |
| {service/host} | {purpose} | `{ENV_KEY}` / {mechanism} | {yes/no/unknown} |

只展示变量名、服务名和脱敏 host/path，绝不展示 token 或 `.env` 值。

## 数据层与配置路径

说明模型/表、缓存键、迁移或初始化入口、配置加载顺序以及环境变量作用。未扫描到的项写 `[待确认: ...]`。

## 常见任务上手路径

以“修改 {功能}”为例：

1. 从 `{route/file:line}` 进入，先阅读 `{function}` 的输入/输出和调用方。
2. 沿图追到 `{orchestrator}` 与 `{executor}`，只在证据确认的文件中修改。
3. 运行 `{test/command}`，检查 `{observable result}`，再更新对应文档或契约。

## 限制与待确认项

记录扫描范围、跳过目录、非标准语言、解析失败、`[UNKNOWN: ...]` 边和需要人工确认的架构假设。
